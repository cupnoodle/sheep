<nav class="pagination">
	{{#if prev}}
    	<a href="{{page_url prev}}" class="newer-posts">← Newer Posts</a>
    {{/if}}
    <span class="page-number">Page {{page}} of {{pages}}</span>
    {{#if next}}
    	<a href="{{page_url next}}" class="older-posts">Older Posts →</a>
    {{/if}}
</nav>